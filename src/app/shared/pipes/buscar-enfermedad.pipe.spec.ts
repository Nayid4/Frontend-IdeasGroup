import { BuscarEnfermedadPipe } from './buscar-enfermedad.pipe';

describe('BuscarEnfermedadPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarEnfermedadPipe();
    expect(pipe).toBeTruthy();
  });
});
