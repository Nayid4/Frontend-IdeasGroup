import { NombreDeUsuarioPipe } from './nombre-de-usuario.pipe';

describe('NombreDeUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreDeUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
