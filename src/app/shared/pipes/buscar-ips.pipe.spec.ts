import { BuscarIPSPipe } from './buscar-ips.pipe';

describe('BuscarIPSPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarIPSPipe();
    expect(pipe).toBeTruthy();
  });
});
